apiVersion: v1
kind: Service
metadata:
  name: payment-service-app
  namespace: payment-service
  labels:
    app: payment-service-app
spec:
  selector:
    app: payment-service-app
  ports:
    - port: 3009
      targetPort: 3009
      nodePort: 30009
  type: NodePort
