apiVersion: v1
kind: Service
metadata:
  name: payment-service-app
  namespace: payment-service
  labels:
    app: payment-service-app
spec:
  selector:
    app: payment-service-app
  ports:
  - port: 3030
    targetPort: 3030
  type: NodePort
